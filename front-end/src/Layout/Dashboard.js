import "../App.js";

function Dashboard() {
  return (
    <>
      <p>This page will show a hello message, maybe a nice friendly quote.</p>
      <p>
        The ability to update clients is on the client page which will have a
        button on each client to allow updating
      </p>
      <p>Maybe it will show a list of active clients as the useEffect</p>
    </>
  );
}

export default Dashboard;
